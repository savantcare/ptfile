module.exports = {
  DIAGNOSIS_API_URL: process.env.baseUrl + '/diagnosis', // Use this at the time of commit in production
  // DIAGNOSIS_API_URL: 'http://localhost:8000/diagnosis', // Use this for local development as NODE_ENV = dev is not working in windows
}
